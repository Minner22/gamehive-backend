# Uruchomienie z profilem: -Dspring.profiles.active=prod
# Domyślnie aktywny profil dev (lokalny development)
spring:
    profiles:
        active: dev
    application:
        name: GameHive
    threads:
        virtual:
            enabled: true
    mail:
        host: smtp.gmail.com
        port: 587
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
    config:
        import: optional:file:./secret.properties
    jpa:
        properties:
          hibernate:
              default_batch_fetch_size: 20

springdoc:
    swagger-ui:
        path: /swagger-ui.html
        operations-sorter: alpha
        tags-sorter: alpha
        display-request-duration: true
    api-docs:
        path: /v3/api-docs

jwt:
    activation:
        validityInSeconds: 86400
    refresh:
        validityInSeconds: 604800
        maxActiveTokensPerUser: 5
    access:
        validityInSeconds: 900
    passwordreset:
        validityInSeconds: 900
gamehive:
  moderation:
      max-resubmissions: 5
  search:
      enabled: true
      host: http://localhost:7700
      index-uid: gamehive_content
      reindex-batch-size: 200
